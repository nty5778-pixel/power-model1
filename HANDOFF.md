# ERCOT DA/RT 조달 모델 — Claude Code 핸드오프

> 이 문서는 **새 세션에서 작업을 이어받기 위한 컨텍스트**다.
> 모델 스펙 상세는 `MODEL_CONFIG.md`, 분석 근거·시사점은 `REVIEW_HISTORY.md` 참조.

---

## 0. 30초 요약

LZ_HOUSTON 전력 조달에서 **DA(전일) / RT(실시간) 매수 비중**을 D+1~D+4로 산출하는 모델.
XGBoost 3개 + 규칙 1개 + 날씨 오버레이. Render(FastAPI) 배포 → n8n 오케스트레이션 → Google Sheets 기록.

**가장 중요한 전제:** 이 모델의 방향 예측력은 **AUC ≈ 0.52**, 사실상 동전 던지기다.
DART 스프레드는 본질적으로 *예측오차(forecast error)* 변수이고, 레벨 변수로는 R² ≈ 1%밖에 설명이 안 된다.
**값어치는 "알파"가 아니라 "분산 감소 + 한쪽 쏠림 방지"에 있다.** 이 전제를 잊고 성능 개선을 시도하면
반드시 과적합으로 끝난다 — 실제로 이 프로젝트에서 그럴듯한 가설이 백테스트로 4번 반증됐다(§5).

---

## 1. 레포 구조 (권장)

```
ercot-dart/
├── run_models_d1_d4_v4_weather.py   # 모델 본체 (CLI + 라이브러리 겸용)
├── app.py                           # FastAPI 래퍼 (Render 배포용)
├── requirements.txt
├── render.yaml
├── data/                            # 학습 데이터 (레포에 커밋 — Render 디스크는 ephemeral)
│   ├── 2024_Historical_Data.csv
│   ├── 2025_Historical_Data.csv
│   ├── 2026_Historical_Data.csv
│   ├── texas_open_meteo_historical_2024_2026-06-30.csv
│   ├── texas_open_meteo_daily.csv   # 예보 (16일)
│   └── GD_Katy.csv                  # 가스 (모델 미사용, 진단용)
├── n8n/
│   ├── n8n_1_daily_predict.json
│   └── n8n_2_backfill_lookback.json
└── docs/
    ├── MODEL_CONFIG.md
    ├── REVIEW_HISTORY.md
    └── HANDOFF.md                   # 이 문서
```

**미사용 데이터:** `Congesiton_*.csv`(존별 부하/지역별 풍력·태양광 5개 파일)는 분석·가설검증에만
썼고 프로덕션 코드에 반영돼 있지 않다. 필요 시 §6-2 참조.

---

## 2. 실행

```bash
pip install -r requirements.txt

# CLI (폴더 내 CSV 자동 탐색)
python run_models_d1_d4_v4_weather.py --folder ./data --volume_mw 100
# → allocation_review.csv 생성 + stdout 표 출력

# Hard Gate 실험 활성화 (기본 OFF, 백테스트상 역효과 — §5 참조)
python run_models_d1_d4_v4_weather.py --folder ./data --use_gate

# API 로컬 실행
DATA_DIR=./data uvicorn app:app --reload --port 8000
curl localhost:8000/health
```

**필수 입력:** `forecast_input.csv` (없으면 학습·정확도 출력 후 종료)

| 컬럼 | 타입 |
|---|---|
| `timestamp` | ISO8601 시간별 |
| `fc_load_mw` | float |
| `fc_wind_mw` | float |
| `fc_solar_mw` | float |
| `gas_price` | (선택) 있으면 `gas_fc`로 사용 |

---

## 3. 데이터 계약 (중요)

### 3.1 ERCOT CSV 컬럼 해석 — 이름 부분매칭

컬럼명이 길고 파일마다 순서가 달라, **하드코딩이 아니라 `NEEDLES` 부분매칭**으로 찾는다
(`_col(cols, *needles)` — 모든 needle이 소문자 포함되면 매치).

```python
NEEDLES = dict(
    DA=("DA LMP",), RT_LZ=("RT SPP", "LZ_HOUSTON"), RT_HB=("RT SPP", "HB_BUSAVG"),
    fc_load=("Forecast - Load", "Prior Day"), fc_solar=("Solar", "Forecast"),
    fc_wind=("Wind", "Forecast"), act_load=("ISO: Actual - Load",),
    act_solar=("Solar", "Actual - Generation"), act_wind=("Wind", "Actual - Generation"),
    env_act_nl=("ENV: Actual - Net Load",), prc=("PRC",))
```

컬럼을 못 찾으면 `ValueError`로 즉시 실패한다(조용한 NaN 전파 방지). 새 데이터 소스를 붙일 때는
NEEDLES만 수정하면 된다.

**파일 판별 규칙**
- ERCOT 과거 데이터 = `DA LMP` 컬럼 보유 CSV (여러 개면 자동 concat)
- 날씨 = `temp_mean_f` + `date` 컬럼 보유 CSV (과거+예보 자동 합침)
- 가스 = 파일명에 `katy|gd_|platts|henry` (헤더 2줄 구조, `skiprows=1`)

### 3.2 타임스탬프
`Timestamp`의 tz 오프셋을 **정규식으로 제거**하고 wall-clock으로 파싱한다
(`[+-]\d{2}:\d{2}$` → ""). DST 때문에 파일마다 오프셋이 −06:00/−05:00으로 다르기 때문.
UTC 변환하면 시간대별 분석이 어긋난다.

### 3.3 net load 정의 — ENV 단일 기준 (변경 금지)
```python
mn["act_netload"] = mn.env_act_nl     # direct(L−W−S) 사용 금지
```
2024는 Actual Load가 **100% 결측**이라 direct 계산이 불가능하다. 예전 "direct 우선 / ENV fallback"
로직은 연도별로 계산 방식이 갈려(2024=ENV, 2025~26=direct) 방식차가 최대 **17.6 GW**까지 났다.
ENV 통일로 학습 행이 12,790 → 21,719 (+70%) 늘었다.

### 3.4 누수 방지 규칙
- 모든 lag/rolling은 `shift(1)` 이후 계산
- vote 정규화 스케일(`dev_c`, `dev_s`), 오버레이 임계(`wx.cold/hot`), 게이트 임계는
  **학습 구간 분포에서만** 산출 — 하드코딩 금지
- ⚠️ `gas_lag1`은 엄밀히는 `lag2`여야 한다(DA 마감 10:00 CT 시점에 D-1 종가는 미확정).
  현재 미수정 — 가스 피처가 모델에서 기각됐으므로(§5) 영향 없으나, 재도입 시 반드시 고칠 것.

---

## 4. 아키텍처 요약

```
v1 = M1.predict_proba()                       # P(DA가 더 쌈) 직접 분류
v2 = 1 − sigmoid((DA예측 − 30일중앙값 − dev_c) / dev_s)
v4 = M4.predict_proba()                       # P(Houston basis > $2)

# --- 배분 규칙 (combine_votes) — 2026-08-14 부터 m1_only 가 기본 ---
base_DA = 1.0 if v1 > 0.50 else 0.0           # m1_only : 평소 RT, 모델1 신호일만 DA
# base_DA = mean(v1, v2, v4)                  # ensemble: 종전 기본. --alloc ensemble 로 사용

if use_gate and gate_signal:                  # 기본 OFF
    f_da = max(base_DA, GATE_FLOOR=0.75)

f_da = weather_overlay(f_da, t_anom, cold_t, hot_t)   # 극단 기온일에만
```

**배분 규칙은 `combine_votes()` 한 곳에만 정의돼 있다.** CLI·API·백테스트가 전부 이 함수를
부른다. 예전에 백테스트가 같은 식을 따로 복제해 갖고 있어 한쪽만 고치면 조용히 어긋났다.

`m1_only` 로 바꾼 근거는 §5 결정 로그 7번. 문턱 0.50 은 **올리면 안 된다**(§5 참조).

| 모델 | 알고리즘 | 타깃 |
|---|---|---|
| M1 | XGBClassifier | `da_cheaper = (RT − DA > 0)`, dead-band $3 + 비용가중 |
| M2 | XGBRegressor | 일평균 DA 가격 |
| M3 | **규칙 (ML 아님)** | reserve/수급 스트레스 게이트 — **기본 OFF** |
| M4 | XGBClassifier | `basis > $2` |

**날씨 오버레이 (비대칭)**
- 이상한파(하위 5%) → **RT로** 기울임 (한파엔 DA가 폭등: 실적 DA $76 vs RT $48)
- 이상고온(상위 5%) → **DA로** 기울임 (폭염엔 RT가 튐: DART +$17)
- 평년 근처(~87%) → 미개입
- `WX_COLD_FLOOR = 0.30`: 한파에도 DA를 이 아래로 내리지 않음. **최적화 대상이 아니라 tail 리스크 정책**

**핵심 상수** (`run_models_d1_d4_v4_weather.py` 상단)
`DEADBAND=3.0`, `CW_CAP=100.0`, `GATE_FLOOR=0.75`, `WX_COLD_Q=0.05`, `WX_HOT_Q=0.95`,
`WX_SPAN_F=5.0`, `WX_COLD_LEAN=0.30`, `WX_HOT_LEAN=0.30`, `WX_COLD_FLOOR=0.30`

---

## 5. 결정 로그 — 반증된 가설 (재시도 금지)

> 아래는 전부 **그럴듯했지만 walk-forward 백테스트가 기각**한 것들이다.
> 다시 시도하기 전에 반드시 이 근거를 넘어설 새 증거가 있어야 한다.

| # | 가설 | 결과 | 근거 |
|---|---|---|---|
| 1 | M1 학습을 2025+로 제한 (레짐 적합성) | **기각** | 원가 +$1.19 → +$1.81 악화. 오래된 레짐이라도 표본이 많은 쪽이 유리 |
| 2 | vote를 분위수(rank) 정규화 | **기각** | 전체 81%가 ±$10 이내 노이즈인데, rank는 그 구간까지 펴서 "무의미한 날에 확신 투표" 유발. sigmoid 채택 |
| 3 | "위험 시 DA 강제" Hard Gate | **기각** | 원가 +$0.2 → +$1.2 악화. ERCOT는 한파에 **DA가 폭등**하므로 가장 비싼 쪽을 강제 매수하는 꼴 |
| 4 | 가스 피처 / implied heat rate 추가 | **기각** | M2 MAE $8.76 → $9.04 악화, 앙상블 원가 ±$0.03(노이즈). 이유: D-2 종가는 한파 스파이크를 못 보고, 평상시엔 `DA_r3/r7`이 이미 가스 정보를 품음 |
| 5 | "RT 스파이크 신호 시 DA 매수" | **구조적 불가** | DA는 전일 10:00 CT 마감. 스파이크를 보고 나서 DA를 살 수 없음 — 반응형이 아니라 예측형만 가능 |
| 6 | 일 단위 저풍량 임계로 DA 헤지 | **기각** | 사후(hindsight) 정보로도 절감 상한 $0.56, 과헤지 시 손해 |

| 7 | 세 표를 1/3씩 평균 (`ensemble`) | **기각** | walk-forward 531일에서 100% RT 대비 **연 −18만달러 손해**. `m1_only`(모델1 단독)는 +30만달러 절약 → 차이 49만달러. M2는 가격 '수준', M4는 지역 간 가격차를 맞히는 모델이라 "DA가 쌀까"에 간접적이고, 평균이 M1 신호를 희석시킨다 |
| 8 | 애매한 날 DA/RT 반반 (3단계 배분) | **기각** | 이진 규칙과 사실상 동일(+29만 vs +30만)한데 규칙만 복잡해진다. 애매한 구간(v1 0.40~0.50)의 평균 DART가 −$0.81/−$0.20으로 **음수**라 그 날 DA를 절반 사면 손해가 쌓인다. 2025년 데이터로 문턱을 고르게 했더니 스스로 폭 0.01(=사실상 반반 없음)을 선택 |
| 9 | 문턱을 올려 "확신 강한 날만" 매수 | **기각** | 0.40~0.50 은 +25~30만으로 평평한데 **0.52부터 +7만, 0.60에서 −8만으로 꺾인다.** v1 구간별 평균 DART가 0.5~0.6에서 +$4.87/+$2.60인데 **0.6 이상에서 −$0.48로 음수**. 이 모델은 점수가 높을수록 좋은 게 아니다 |
| 10 | 순부하를 direct(L−W−S)로 전환 | **보류** | 정의 문제가 아니라 데이터 문제. 현재 소스는 2024년 부하 실측이 통째로 없어 유효율 58.9% → m1_only 절약이 +30만에서 **−39만으로 뒤집힌다**(큰날 적중 60.0%→33.3%). 코드는 `--netload direct` 로 준비됨. **ERCOT API로 2024 부하 실측을 채워 유효율 95% 이상이 되면 재측정할 것** |

**채택된 것**
- M1 타깃을 `P(DA>RT)` 직접 분류로 (원가 −$0.29, "94% DA 쏠림" 병리 제거)
- NFE ENV 단일화 + P1~P99 winsorize
- 날씨 anomaly를 M1 피처로 (1월 구간 −$1.81, 변동성 38.4→32.5) — **오버레이보다 피처가 주역**
- **배분 규칙 `m1_only`** (2026-08-14): 평소 RT, 모델1 점수 > 0.50 인 날만 DA.
  RT 대비 원가 −$0.345/MWh, 변동성 17.2→15.3 — **요금과 출렁임을 동시에 이긴 첫 규칙**
- 날씨 오버레이는 켠 채로 유지 (+30.2만 vs 끄면 +29.6만, 그리고 극단 기온 안전장치 역할)

---

## 6. 현재 성능

> **재현 방법이 생겼다.** 이 절의 수치는 전부 `backtest_walkforward.py` 로 다시 뽑을 수 있다.
> 예전에는 검증 코드가 레포에 없어 표를 확인할 방법이 없었다. 배분 규칙 변경 제안은
> 반드시 이 도구를 통과해야 한다(§10).
> ```
> python backtest_walkforward.py --folder ./data
> ```

### 6.1 현행 기본 규칙 `m1_only` (walk-forward 531일, 2025-01-10~2026-06-24, 단순평균 $/MWh)

| 전략 | 실현원가 | 변동성 | RT 대비 | \|DART\|≥$20 적중 |
|---|---|---|---|---|
| **m1_only (기본)** | **$32.93** | **15.32** | **−$0.345** | **60.0%** |
| 100% RT | $33.28 | 17.16 | 0 | 40.0% |
| 100% DA | $35.37 | 34.49 | +$2.095 | 60.0% |
| 고정 50/50 | $34.33 | 23.35 | +$1.048 | 40.0% |
| ensemble (종전) | $33.50 | 15.74 | +$0.221 | 40.0% |

100MW 환산 시 RT 대비 **연 +30만달러 절약**, ensemble 대비 **+49만달러**.
531일 중 DA를 산 날은 178일(34%), 나머지는 RT 100%.
**요금과 변동성을 동시에 100% RT보다 낮춘 첫 규칙이다.**

> ⚠️ `hit_rate_big20` 만으로는 부족하다. 100% DA도 60.0%가 나오는데(그 30일 중 18일은
> 실제로 DA가 쌌으므로), 원가는 오히려 $2.09/MWh 비싸다. 그래서 look-back 경보는
> **적중률 < 50%** 와 **`vs_rt_big20` > 0**(그 날들의 실현원가가 RT보다 나쁨)을 함께 본다.
> m1_only 는 같은 60.0%를 **DA를 34%의 날에만 사면서** 달성하고 `vs_rt_big20` = −$8.77 이다.

### 6.2 이익이 어디서 나오는가 — 이게 가장 중요하다

| \|DART\| 구간 | 일수 | m1_only 적중 | 항상 RT 적중 | RT 대비 원가 |
|---|---|---|---|---|
| $5 미만 | 281 | 52.3% | **62.3%** | +$0.45 (열위) |
| $5~20 | 220 | 58.6% | **62.7%** | +$0.10 (열위) |
| **$20 이상** | **30** | **60.0%** | 40.0% | **−$8.77 (우위)** |

**절약의 대부분이 531일 중 30일(5.6%)에서 나온다.** 나머지 501일은 소폭 열위다.
그래서 운영 감시 지표는 `hit_rate_big20` 하나다(§9, README `lookback` 참조).

⚠️ **전체 적중률로 판단하지 말 것.** 이 지표는 항상 RT가 61.2%로 m1_only(55.4%)를 이긴다.
$0.5 날과 $50 날을 동일하게 세기 때문이다. 실제 돈은 반대 방향이다.

### 6.3 통계적 유의성

부트스트랩 2만회: 절약 +30만달러, **95% 구간 −20만 ~ +88만, 손해로 끝날 확률 13%**.
다만 같은 일수를 무작위로 골라 DA를 사면 중앙값 −52만달러 손해이고, 모델이 무작위보다
나을 확률은 **99.1%** 다. → **실력은 있으나 "확실히 이득"이라 말할 표본은 아직 아니다.**

연도별로는 2025년 +9만 / 2026년 +71만으로 **거의 전부 2026년에서 나왔다.** §8-1 참조.

---

## 7. 알려진 이슈 / 정리 필요

| 위치 | 내용 | 우선순위 |
|---|---|---|
| `daily_panel` | `gas_lag1` → `gas_lag2`로 교정 필요. DA 마감 10:00 CT 시점에 D-1 종가는 미확정 | **중간** — M2가 실제로 `gas_lag1`/`gas_r7`을 쓰므로 유효한 누수다 |
| `app.py` | 학습이 요청당 발생(10~30초), 날짜 단위 메모리 캐시만 존재 → 콜드스타트 시 무효 | 중간 |
| n8n Sheets `update` | 행 수천 개 넘으면 전체 스캔으로 느려짐 → 연 단위 시트 분할 또는 DB 이전 | 중간 |
| 순부하 정의 | `--netload direct` 준비됨. 2024 부하 실측이 채워지면 재측정 (§5-10) | 중간 |
| `forecast_daily` | `agg` 지역변수 미사용 | 낮음 |

**해소된 것 (2026-08-14)**

| 내용 | 처리 |
|---|---|
| 날씨 파일이 `.txt` 라 인식 안 됨 → 오버레이·피처 전 구간 비활성 | `list_csvs` 가 `.txt` 도 스캔 |
| `Congesiton_*.csv` 가 학습 데이터로 오인되어 2025~26 피처를 통째로 NaN 화 | `is_ercot_history()` 가 필수 컬럼 전부 확인 |
| 계절 기준선이 2.5년치 자체 산출 | 시트가 주는 30년 NOAA 평년값(`normal_temp_mean_f`) 우선 사용 |
| 같은 날짜 중복 시 오래된 예보가 최신 실측을 덮어씀 | 실측 > 예보, 동률이면 최신 `run_id` 우선 |
| `run()` 의 `f1, mdl1 = ...` dead code | 제거 |
| 백테스트가 배분식을 따로 복제 → 한쪽만 고치면 어긋남 | `combine_votes()`/`allocate()` 단일 정의로 통합 |
| 안 쓰는 컬럼 3개(`act_load`/`act_solar`/`act_wind`)를 필수로 요구 | `CORE` + `NL_NEEDS` 로 분리, PRC도 선택으로 |

---

## 8. 한계 — 코드 수정으로 해결되지 않는 것

1. **표본 의존성 (최대 취약점).** §6.2 대로 **531일 중 30일이 이익의 대부분**을 만든다.
   연도로 봐도 2025년 +9만 / 2026년 +71만으로 사실상 2026년 한 해의 결과다.
   2021 Uri(RT $9,000 폭등), 2023 Elliott을 넣으면 **"한파→RT" 방향이 반대로 작동할 수 있다.**
   `WX_COLD_FLOOR`를 남긴 이유가 정확히 이것이다.
2. **성과가 계절을 탄다 — "2026년이 특별"한 게 아니었다.** (2026-08-14 규명)

   기존에 "2025년 +9만 / 2026년 +71만" 으로 보였던 것은 **비교 자체의 착시**였다.
   2025년은 12개월, 2026년은 1~6월이라 계절이 섞여 있었다. 같은 달끼리 맞추면:

   | 구간 | 일수 | 절약(천달러/년) | \|DART\|≥$20 적중 |
   |---|---|---|---|
   | 2025년 1~6월 | 172 | **+690** | 62% |
   | 2026년 1~6월 | 175 | **+733** | 70% |
   | 2025년 7~12월 | 184 | **−469** | 43% |

   **두 해의 상반기는 사실상 같다.** 연도 차이가 아니라 **상반기(+712) vs 하반기(−469)** 다.

   하반기 손실은 **7월 한 달(−1,977)이 지배**하고, 그 7월 손실은 다시 **며칠이 지배**한다 —
   상위 3일을 빼면 −1,977 → −515 로 줄어든다. 특히 2025-07-30 은 DA $92.22 / RT $53.85 로
   하루에 $38 을 잃었다. 7월에 DA 를 산 8일 중 **이득 본 날이 0일**이었다.

   가스가격·재생비중·예보정확도는 같은 1~6월끼리 비교해도 뚜렷한 변화가 없었다
   (가스 3.15→2.97, 재생 0.44→0.47, 예보 절대오차 2,522→2,722MW 로 오히려 악화).
   유일하게 눈에 띄는 변화는 예보 순부하 평균 −1,762MW(재생 증가분)인데 설명력은 불명이다.

   ⚠️ **하반기 표본은 2025년 한 해(184일)뿐이다.** "여름에는 안 통한다" 인지
   "2025년 여름이 나빴다" 인지 구분할 수 없다. 7월은 31일 × 1년이다.
   **이 관찰로 계절별 규칙(예: 7~8월엔 DA 금지)을 만들면 §10 위반이다.**
   대신 (a) 성과 리뷰를 반기로 나눠 보고, (b) 하반기 부진을 곧바로 고장으로 읽지 말고,
   (c) 2022~2023 여름 데이터 확보를 우선하라(§9-2).
3. **여름 이상고온 표본 6일.** 폭염 오버레이(+DA)는 방향은 그럴듯하나 통계 근거가 얇다.
4. **forward regime 부재.** M3·M4의 PRC·basis는 D0 값을 D+1~D+4에 복사한다. 발전기 트립,
   한파 급습 같은 미래 surprise를 반영 못 한다.
5. **원가차의 신뢰구간이 0을 걸친다.** §6.3 — 손해로 끝날 확률 13%. bootstrap 은 수행됨.
6. **RT basis와 DART의 상관(+0.18, 낮 +0.75)은 내생성.** 둘 다 `RT_LZ`를 공유하므로 예측 변수로 쓰면 안 된다.

---

## 9. 다음 작업 후보 (우선순위)

1. **2022~2023 데이터 추가 (특히 여름)** — §8-1, §8-2를 동시에 해소하는 유일한 방법.
   현재 하반기 표본은 2025년 한 해뿐이라 "여름에 안 통한다"를 판정할 수 없다. **최우선.**
   ~~2026년 레짐 변화 규명~~ → 2026-08-14 완료. 레짐 변화가 아니라 계절 효과였다(§8-2).
3. **congestion 데이터 통합** — 존별 부하·지역별 풍력으로 M4(basis) 재학습.
   (구조: 부하는 동부 83%, 풍력은 서부 69% → west→east 혼잡이 basis의 원천)
   ⚠️ 학습용 시계열과 지역별 피처 소스를 코드에서 명확히 분리한 뒤 할 것.
4. **EEA / 강제정지 forward** — 극단 레짐 조기 신호. §8-4 를 구조적으로 해소한다.
5. **`WX_COLD_FLOOR` 민감도 표** — 0.30 / 0.15 / 0 에서 ≥$20 적중률 vs tail 노출 트레이드오프.
   *리스크 정책 결정이므로 코드가 아니라 사람이 판단할 자료를 만드는 작업.*
6. **예보 입력 범위 검증** — XGBoost는 학습 범위 밖을 외삽하지 못하고 가장 가까운 값으로 수렴한다.
   `forecast_input.csv` 값이 학습 범위를 벗어나면 경고를 띄울 것. §8-3(여름 표본 부족)과 맞물린다.
7. **파이프라인 배선** — `ERCOT_FORECAST_URL`, `ERCOT_SETTLE_URL`을 사내 소스에 연결.
   ⚠️ 정산 소스는 반드시 **authoritative**여야 한다. TimeSeriesExport 파생값은 실제 정산치와
   괴리가 확인됐고, 쓰면 look-back 지표 전체가 오염된다.

**ERCOT API 연동 시 필요한 원본 항목** (2026-08 기준, `NEEDLES` / `CORE` 참조)

| 구분 | 항목 |
|---|---|
| 항상 필수 (6) | DA LMP(LZ_HOUSTON), RT SPP(LZ_HOUSTON), RT SPP(HB_BUSAVG), 부하 예보(전일), 풍력 예보, 태양광 예보 |
| `netload=env` 추가 | ENV Net Load |
| `netload=direct` 추가 | 부하 실측, 풍력 실측, 태양광 실측 — **2024년까지 채워야 의미 있음** |
| 선택 | PRC (M3 게이트 전용, 기본 OFF) |
| ERCOT 외부 | 가스 가격(Platts Katy) — M2가 실제 사용, 날씨(30년 평년 포함) |

---

## 10. 작업 규칙 (이 프로젝트의 컨벤션)

- **설계 논쟁은 백테스트로 결판낸다.** 직관이 10번 반증됐다(§5). 변경 제안은 동일 OOS 비교
  수치와 함께. 검증 도구는 `backtest_walkforward.py` 이고, 이제 레포 안에 있다.
- **평가는 AUC가 아니라 비용으로.** $50 날과 $0.5 날을 동등 취급하는 지표는 이 문제에 부적합.
  실현 원가 + \|DART\|≥$20 적중률을 본다.
  ⚠️ **전체 적중률은 함정이다.** 아무것도 안 하고 RT만 사면 61.2%로 항상 1등이 나오는데,
  그 선택은 연 30만달러를 버린다(§6.2). 적중률로 규칙을 고르면 반드시 틀린 답이 나온다.
- **파라미터 자동 튜닝 금지.** AUC 0.52 모델에서 30~60일 성과는 거의 전부 노이즈다.
  look-back은 모니터링·축적용이고, 변경은 분기별 전체 walk-forward 후 사람이 결정한다.
- **basis는 평균이 아니라 중앙값으로.** 한파월 평균은 극단 혼잡 스파이크가 지배한다
  (1월 평균 −$8.4 vs 중앙값 −$0.2).
- **1월(한파)은 항상 별도 레짐으로 분리해서 본다.** 통합 평균은 오독을 부른다.
- **연도끼리 비교할 때는 같은 달끼리 맞춘다.** 2026년은 1~6월치뿐이라 12개월인 2025년과
  그냥 비교하면 계절이 섞여 없는 차이가 보인다 — 실제로 그렇게 오독한 적이 있다(§8-2).
- 실패는 조용한 NaN보다 예외가 낫다(컬럼 미발견 시 `ValueError`, 예보 24행 미만 시 n8n에서 throw).
